export default interface User {
  username: string
  firstName: string
  lastName: string
  country: string
  language: string
}

export type WindowInfo = {
  appName: string
  URL: string
  isProductive: boolean
}
